---
type Props = {
  title: string;
  text: string;
  buttonLabel: string;
  buttonHref: string;
};

const { title, text, buttonLabel, buttonHref } = Astro.props as Props;
---

<div
  class="mt-14 grid justify-items-center gap-8 border border-brand-gold/30 bg-darkSurface p-8 text-center text-white lg:grid-cols-[1fr_auto] lg:items-center lg:justify-items-stretch lg:text-left lg:p-9"
>
  <div class="max-w-3xl">
    <h3
      class="font-libertinus text-3xl font-semibold leading-tight text-brand-gold"
    >
      {title}
    </h3>

    <p class="mt-3 font-inter leading-relaxed text-white/75">
      {text}
    </p>
  </div>

  <a href={buttonHref} class="button-primary w-fit lg:justify-self-end">
    {buttonLabel}
  </a>
</div>