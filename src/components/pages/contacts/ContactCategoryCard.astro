---
import Icon from "../../ui/icons/Icon.astro";
import type { IconName } from "../../ui/icons/iconPaths";

type Props = {
  icon: IconName;
  title: string;
  text: string;
  button: string;
  href: string;
};

const { icon, title, text, button, href } = Astro.props as Props;
---

<article class="contact-method-card">
  <div class="contact-method-card__icon" aria-hidden="true">
    <Icon name={icon} />
  </div>

  <h2 class="contact-method-card__title">
    {title}
  </h2>

  <p class="contact-method-card__text">
    {text}
  </p>

  <a class="button-primary contact-method-card__button" href={href}>
    {button}
  </a>
</article>